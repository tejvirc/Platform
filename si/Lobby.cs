namespace Aristocrat.Monaco;

public class Lobby
{
    private readonly IPropertiesManager _properties;

    public Lobby(IPropertiesManager properties)
    {
        _properties = properties;
    }
}
