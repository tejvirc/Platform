<operatorMenu:OperatorMenuPage x:Class="Aristocrat.Monaco.Application.UI.Views.DoorPage"
                               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                               xmlns:converters="clr-namespace:Aristocrat.Monaco.UI.Common.Converters;assembly=Aristocrat.Monaco.UI.Common"
                               xmlns:viewModels="clr-namespace:Aristocrat.Monaco.Application.UI.ViewModels"
                               xmlns:operatorMenu="clr-namespace:Aristocrat.Monaco.Application.UI.OperatorMenu"
                               xmlns:lex="http://wpflocalizeextension.codeplex.com"
                               xmlns:loc="http://monaco.aristocrat.com/localization"
                               mc:Ignorable="d"
                               d:DesignHeight="300"
                               d:DesignWidth="300"
                               d:DataContext="{d:DesignInstance viewModels:DoorPageViewModel}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Aristocrat.Monaco.UI.Common;component/Skins/OperatorMenu.xaml" />
                <ResourceDictionary Source="/Aristocrat.Monaco.UI.Common;component/Skins/Converters.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:DateTimeFormatConverter x:Key="DateTimeFormat" />
        </ResourceDictionary>
    </UserControl.Resources>
    <DataGrid ItemsSource="{Binding Doors}"
              AutoGenerateColumns="False"
              CanUserResizeColumns="False"
              CanUserReorderColumns="False"
              CanUserSortColumns="False"
              IsReadOnly="True"
              AutomationProperties.AutomationId="DataGrid0_C063434">
        <DataGrid.Columns>
            <DataGridTextColumn Header="{lex:Loc Key={x:Static loc:ResourceKeys.NameLabel}}"
                                loc:Localizer.For="Operator"
                                MinWidth="250"
                                Binding="{Binding Name, Mode=OneWay}"
                                AutomationProperties.AutomationId="DataGridTextColumn1_C063434" />
            <DataGridTextColumn Header="{lex:Loc Key={x:Static loc:ResourceKeys.ActionLabel}}"
                                loc:Localizer.For="Operator"
                                MinWidth="150"
                                Binding="{Binding Action, Mode=OneWay}"
                                AutomationProperties.AutomationId="DataGridTextColumn2_C063434" />
            <DataGridTemplateColumn Header="{lex:Loc Key={x:Static loc:ResourceKeys.LastOpenedLabel}}"
                                    loc:Localizer.For="Operator"
                                    MinWidth="150"
                                    AutomationProperties.AutomationId="DataGridTemplateColumn3_C063434">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate DataType="{x:Type viewModels:DoorViewModel}">
                        <StackPanel Orientation="Vertical" AutomationProperties.AutomationId="StackPanel0_3CBC5210">
                            <TextBlock Width="Auto"
                                       Visibility="{Binding Ignored, Converter={StaticResource TrueCollapsedFalseVisible}}"
                                       Text="{Binding LastOpened, Converter={StaticResource DateTimeFormat}}"
                                       AutomationProperties.AutomationId="TextBlock4_C063434" />
                            <TextBlock Width="Auto"
                                       Visibility="{Binding Ignored, Converter={StaticResource TrueVisibleFalseCollapsed}}"
                                       Text="{lex:Loc Key={x:Static loc:ResourceKeys.NotAvailable}}"
                                       AutomationProperties.AutomationId="TempId_8585" />
                        </StackPanel>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Binding="{Binding Message, Mode=OneWay}"
                                Foreground="Yellow"
                                AutomationProperties.AutomationId="DataGridTextColumn6_C063434" />
        </DataGrid.Columns>
    </DataGrid>
</operatorMenu:OperatorMenuPage>