namespace Aristocrat.SasClient.Tests
{
    // common constants used in the unit tests
    public static class TestConstants
    {
        public const byte SasAddress = 0x01;
        public const byte FakeCrc = 0xFF;
        public const byte Nack = 0x80;
        public const byte BusyResponse = 0x00;
    }
}