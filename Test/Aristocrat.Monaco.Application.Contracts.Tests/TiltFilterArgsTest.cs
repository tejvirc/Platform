namespace Aristocrat.Monaco.Application.Contracts.Tests
{
    using Microsoft.VisualStudio.TestTools.UnitTesting;

    /// <summary>
    ///     Tests for TiltFilterArgs
    /// </summary>
    [TestClass]
    public class TiltFilterArgsTest
    {/*
        [TestMethod]
        public void Constructor1Test()
        {
            var target = new TiltFilterArgs();

            Assert.IsNotNull(target);
            Assert.AreEqual(EventDescriptionProperty.Default, target.Property);
            Assert.AreEqual(string.Empty, target.Value);
            Assert.IsFalse(target.Inclusion);
        }

        [TestMethod]
        public void Constructor2Test()
        {
            var value = "Test";
            var inclusion = true;
            var property = EventDescriptionProperty.Level;

            var target = new TiltFilterArgs(property, value, inclusion);

            Assert.IsNotNull(target);
            Assert.AreEqual(property, target.Property);
            Assert.AreEqual(value, target.Value);
            Assert.IsTrue(target.Inclusion);
        }

        [TestMethod]
        public void EqualityTest()
        {
            var target1 = new TiltFilterArgs();
            var target2 = new TiltFilterArgs();
            var target3 = new TiltFilterArgs(EventDescriptionProperty.Level, "Test", true);

            // test with null object
            Assert.IsFalse(target1.Equals(null));

            // test with wrong type
            Assert.IsFalse(target1.Equals(string.Empty));

            // test with matching objects
            Assert.IsTrue(target1.Equals(target2));

            // test with different objects
            Assert.IsFalse(target1.Equals(target3));
        }

        [TestMethod]
        public void GetHashCodeTest()
        {
            var target = new TiltFilterArgs();

            // since we get different hashcodes on different
            // machines, just run it to get coverage and don't check return
            target.GetHashCode();
        }*/
    }
}