<operatorMenu:OperatorMenuPage x:Class="Aristocrat.Monaco.Gaming.UI.Views.OperatorMenu.SingleGameSetupView"
                               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                               xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                               xmlns:operatorMenu="clr-namespace:Aristocrat.Monaco.Application.UI.OperatorMenu;assembly=Aristocrat.Monaco.Application.UI"
                               xmlns:viewModels="clr-namespace:Aristocrat.Monaco.Gaming.UI.ViewModels.OperatorMenu"
                               xmlns:converters="clr-namespace:Aristocrat.Monaco.Gaming.UI.Converters"
                               xmlns:lex="http://wpflocalizeextension.codeplex.com"
                               xmlns:loc="http://monaco.aristocrat.com/localization"
                               mc:Ignorable="d" 
                               Background="Transparent" 
                               d:DataContext="{d:DesignInstance {x:Type viewModels:SingleGameSetupViewModel}, IsDesignTimeCreatable=True}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Aristocrat.Monaco.UI.Common;component/Skins/OperatorMenu.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <converters:FormattedCurrencyConverter x:Key="CurrencyConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="1" Margin="0 15 0 0" 
               Content="{lex:Loc Key={x:Static loc:ResourceKeys.Paytable}}"/>
        <ComboBox Grid.Row="2"
                  ItemsSource="{Binding PaytableOptions}"
                  SelectedItem="{Binding SelectedPaytable}"/>

        <Label Grid.Row="3" Margin="0 5 0 0" 
               Content="{lex:Loc Key={x:Static loc:ResourceKeys.Denomination}}"/>
        <ComboBox Grid.Row="4" Margin="0 0 0 5" 
                  ItemsSource="{Binding DenomOptions}"
                  SelectedItem="{Binding SelectedDenom}"/>

        <Label  Grid.Row="5" Margin="0 5 0 0" 
                Content="{lex:Loc Key={x:Static loc:ResourceKeys.PlaywayOption}}"/>
        <ComboBox Grid.Row="6" Margin="0 0 0 5" 
                  ItemsSource="{Binding PlaywayOptions}"
                  SelectedItem="{Binding SelectedPlayway}"/>

        <Label  Grid.Row="7" Margin="0 5 0 0" 
                Content="{lex:Loc Key={x:Static loc:ResourceKeys.BetOption}}"/>
        <ComboBox Grid.Row="8" Margin="0 0 0 5" 
                  ItemsSource="{Binding BetOptions}"
                  SelectedItem="{Binding SelectedBet}"/>

        <Grid Grid.Row="9" Margin="0 20" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="{lex:Loc Key={x:Static loc:ResourceKeys.RTP}}"/>
            <TextBlock Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Margin="10 0 0 0" 
                       Text="{Binding RTP}"/>

            <Label Grid.Row="1" Grid.Column="0" Content="{lex:Loc Key={x:Static loc:ResourceKeys.MaxBet}}"/>
            <TextBlock Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="10 0 0 0" 
                       Text="{Binding MaxBet}"/>

            <Label Grid.Row="2" Grid.Column="0" Content="{lex:Loc Key={x:Static loc:ResourceKeys.TopAward}}"/>
            <TextBlock Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Margin="10 0 0 0" 
                       Text="{Binding TopAward, Converter={StaticResource CurrencyConverter}, ConverterParameter=ZeroNotAvailable, Mode=OneWay}" />
        </Grid>
    </Grid>
</operatorMenu:OperatorMenuPage>

