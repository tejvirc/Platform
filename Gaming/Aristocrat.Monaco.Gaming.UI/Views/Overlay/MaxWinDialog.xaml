<common:BaseWindow x:Class="Aristocrat.Monaco.Gaming.UI.Views.Overlay.MaxWinDialog"
                   xmlns:common="http://monaco.aristocrat.com/common"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:viewModels="clr-namespace:Aristocrat.Monaco.Gaming.UI.ViewModels"
                    xmlns:converters="http://monaco.aristocrat.com/common"
                    xmlns:controls="clr-namespace:Aristocrat.Monaco.Gaming.UI.Views.Controls"
                    xmlns:gamingConverters="clr-namespace:Aristocrat.Monaco.Gaming.UI.Converters"
                    mc:Ignorable="d"
                    WindowStartupLocation="Manual"
                    ResizeMode="NoResize"
                    WindowStyle="None"
                    Title="OverlayWindow"
                    ShowInTaskbar="False"
                    AllowsTransparency="True"
                    d:DataContext="{d:DesignInstance {x:Type viewModels:MaxWinDialogViewModel}}"
                    IsVisibleChanged="MaxWinDialog_IsVisibleChanged" 
                    d:DesignHeight="450" d:DesignWidth="800">
    <Window.Background>
        <SolidColorBrush x:Name="backgroundColor"  Color="Black" Opacity=".35" />
    </Window.Background>
    <Window.Resources>
        <ResourceDictionary>
            <gamingConverters:ScaleConverter x:Key="ScaleConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    <Viewbox HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid x:Name="ViewInjectionRoot" Width="auto" Height="auto">
            <Image x:Name="BackgroundImage"
               Stretch="uniform"
               converters:ImageHelper.SourceResourceKey="HandPayImage"
               VerticalAlignment="Center" HorizontalAlignment="Center"
               Visibility="Visible"/>
            <Viewbox HorizontalAlignment="Center" VerticalAlignment="Center">
                <Viewbox.Style>
                    <Style TargetType="Viewbox">
                        <Setter Property="Stretch" Value="Uniform"/>
                        <Setter Property="Height" Value="{Binding ElementName=BackgroundImage, Path=ActualHeight, Converter={StaticResource ScaleConverter}, ConverterParameter=0.4}"/>
                        <Setter Property="Width" Value="{Binding ElementName=BackgroundImage, Path=ActualWidth, Converter={StaticResource ScaleConverter}, ConverterParameter=0.75}"/>
                    </Style>
                </Viewbox.Style>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Vertical" Margin="60,26,0,0" VerticalAlignment="Top" Grid.Row="0">
                        <controls:TextPath Width="450" HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontSize="16" FontFamily="Arial" FontWeight="ExtraBold" 
                               Fill="Gold" Stroke="Black" StrokeThickness="0.3"
                               Text="{Binding MaxWinAmount, StringFormat= 'CONGRATULATIONS YOU HAVE WON $\{0\}'}"/>
                        <controls:TextPath  Width="450" Margin="-8,10,0,0" HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontSize="16" FontFamily="Arial" FontWeight="ExtraBold" 
                               Fill="Gold" Stroke="Black" StrokeThickness="0.3"
                               Text=" MAX WIN HAS BEEN REACHED." />
                    </StackPanel>
                </Grid>
            </Viewbox>
        </Grid>
    </Viewbox>
</common:BaseWindow>
