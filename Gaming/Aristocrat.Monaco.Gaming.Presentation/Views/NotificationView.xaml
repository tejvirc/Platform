<UserControl x:Class="Aristocrat.Monaco.Gaming.Presentation.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:views="clr-namespace:Aristocrat.Monaco.Gaming.Presentation.Views"
             xmlns:viewModels="clr-namespace:Aristocrat.Monaco.Gaming.Presentation.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:NotificationViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Viewbox HorizontalAlignment="Right"
                                 VerticalAlignment="Bottom"
                                 AutomationProperties.AutomationId="Viewbox103_1F333AB0">
            <Border Background="#FF000000"
                                    BorderThickness="0"
                                    CornerRadius="6"
                                    Margin="0 4 0 4"
                                    Padding="4 0 4 0"
                                    Visibility="{Binding IsNotificationTextVisible, Converter={StaticResource TrueVisibleFalseHidden}}"
                                    AutomationProperties.AutomationId="Border104_1F333AB0">
                <Label Content="{Binding CurrentNotificationText}"
                                       FontSize="16"
                                       FontFamily="Impact"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       AutomationProperties.AutomationId="Label105_1F333AB0" />
            </Border>
        </Viewbox>
    </Grid>

</UserControl>
