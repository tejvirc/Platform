namespace Aristocrat.Monaco.Gaming.Presentation.Store
{
    /// <summary>
    ///     Action published when banner idle text timer ticks (time to show idle text if appropriate)
    /// </summary>
    public record BannerIdleTextTimerAction
    {
    }
}
