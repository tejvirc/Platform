<UserControl x:Class="Aristocrat.Monaco.Gaming.Lobby.Views.LobbyMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:views="clr-namespace:Aristocrat.Monaco.Gaming.Lobby.Views"
             xmlns:viewModels="clr-namespace:Aristocrat.Monaco.Gaming.Lobby.ViewModels"
             xmlns:regions="clr-namespace:Aristocrat.Monaco.Gaming.Lobby.Regions"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:LobbyMainViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0">
            <Viewbox>
                <!--<views:ChooserView />-->
                <ContentControl prism:RegionManager.RegionName="{x:Static views:RegionNames.Chooser}" /> 
            </Viewbox>
        </Border>
        <Border Grid.Row="1">
            <Viewbox>
                <!--<views:BannerView />-->
                <ContentControl prism:RegionManager.RegionName="{x:Static views:RegionNames.Banner}" />
            </Viewbox>
        </Border>
        <Border Grid.Row="2">
            <Viewbox>
                <!--<views:MultiLanguageUpiView />-->
                <ContentControl prism:RegionManager.RegionName="{x:Static views:RegionNames.Upi}" />
            </Viewbox>
        </Border>
        <Border Grid.Row="3">
            <Viewbox>
                <!--<views:InfoBarView />-->
                <ContentControl prism:RegionManager.RegionName="{x:Static views:RegionNames.InfoBar}" />
            </Viewbox>
        </Border>
        <Border Grid.Row="4">
            <Viewbox>
                <!--<views:ReplayNavView />-->
                <ContentControl prism:RegionManager.RegionName="{x:Static views:RegionNames.ReplayNav}" />
            </Viewbox>
        </Border>
    </Grid>

</UserControl>
