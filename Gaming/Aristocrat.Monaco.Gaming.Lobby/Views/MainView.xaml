<UserControl x:Class="Aristocrat.Monaco.Gaming.Lobby.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:views="clr-namespace:Aristocrat.Monaco.Gaming.Lobby.Views"
             xmlns:viewModels="clr-namespace:Aristocrat.Monaco.Gaming.Lobby.ViewModels"
             xmlns:mediaDisplay="clr-namespace:Aristocrat.Monaco.Gaming.UI.Views.MediaDisplay;assembly=Aristocrat.Monaco.Gaming.UI"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <DockPanel LastChildFill="True">
            <TextBlock DockPanel.Dock="Top" Text="{Binding}" Foreground="White" FontSize="50px" />
            <!--<mediaDisplay:LayoutTemplateDockPanel x:Name="LayoutTemplate" ScreenType="Primary">-->
            <Grid x:Name="GameLayout"
              ClipToBounds="True">
                <Viewbox x:Name="ViewBoxLobby" Stretch="Uniform">
                    <ContentControl prism:RegionManager.RegionName="{x:Static views:RegionNames.Main}" />
                    <!--<ContentControl regions:RegionManager.Region="{x:Static views:RegionNames.Main}" />-->
                </Viewbox>
            </Grid>
        <!--</mediaDisplay:LayoutTemplateDockPanel>-->
        </DockPanel>
    </Grid>
    
</UserControl>
