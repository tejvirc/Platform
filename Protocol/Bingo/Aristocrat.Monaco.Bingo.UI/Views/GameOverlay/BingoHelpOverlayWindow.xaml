<metro:MetroWindow x:Class="Aristocrat.Monaco.Bingo.UI.Views.GameOverlay.BingoHelpOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:metro="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:Aristocrat.Monaco.Bingo.UI.ViewModels.GameOverlay"
        xmlns:cefsharp="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        mc:Ignorable="d" 
        d:DataContext="{d:DesignInstance {x:Type viewModels:BingoHelpOverlayViewModel}}"
        Loaded="BingoHelpOverlayWindow_OnLoaded"
        Title="Bingo Help Overlay"
        ShowInTaskbar="False"
        Width="1440"
        Height="810"
        ShowTitleBar="False"
        ShowIconOnTitleBar="False"
        ShowCloseButton="False"
        ShowMaxRestoreButton="False"
        ShowMinButton="False"
        IgnoreTaskbarOnMaximize="True"
        BorderThickness="0">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Aristocrat.Monaco.UI.Common;component/Skins/Converters.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Visibility="{Binding Visible, Converter={StaticResource BoolToVisConverter}}"
          Height="{Binding Height, Mode=OneWayToSource}"
          Width="{Binding Width, Mode=OneWayToSource}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Margin="{Binding HelpBoxMargin}"
                BorderBrush="{Binding ColorBrush}"
                BorderThickness="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <cefsharp:ChromiumWebBrowser x:Name="BingoHelp" Address="{Binding Address, Mode=OneWay}"/>
        </Border>

        <metro:ProgressRing HorizontalAlignment="Center" VerticalAlignment="Center" IsActive="{Binding IsLoading}" Foreground="White"/>
    </Grid>
</metro:MetroWindow>
