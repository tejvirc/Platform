namespace Aristocrat.G2S.Client.Devices
{
    /// <summary>
    ///     Provides a mechanism for defining a single device class.  The EGM MUST expose only one of each type of a device
    ///     that is defined as a single device.
    /// </summary>
    public interface ISingleDevice
    {
    }
}